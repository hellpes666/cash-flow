# Cash Flow Frontend

Клиентское приложение на Next.js + React + TypeScript для управления личными финансами.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- npm/yarn/pnpm

### Установка

1. **Установите зависимости:**

    ```bash
    npm install
    ```

2. **Запустите dev сервер:**
    ```bash
    npm run dev
    ```

Приложение будет доступно на `http://localhost:3000`

## 📁 Структура проекта

```
frontend/
├── app/                # Next.js App Router
│   ├── layout.tsx      # Корневой layout
│   └── page.tsx        # Главная страница
├── features/           # Бизнес-логика (FSD)
├── widgets/            # Композитные компоненты
│   ├── header/         # Шапка сайта
│   └── not-found-content/
├── shared/             # Переиспользуемые компоненты
│   ├── ui/             # UI компоненты
│   │   ├── button/     # Кнопки
│   │   ├── typography/ # Типографика
│   │   └── empty/      # Пустые состояния
│   └── lib/            # Утилиты
└── public/             # Статические файлы
```

## 🏛️ Архитектура

### Feature-Sliced Design (FSD)

Проект организован по методологии FSD:

- **app/** — инициализация приложения, роутинг
- **features/** — бизнес-логика, фичи приложения
- **widgets/** — композитные компоненты (header, sidebar и т.д.)
- **shared/** — переиспользуемые компоненты и утилиты

### Используемые практики

- **Component-Based Architecture** — модульные компоненты
- **Server-Side Rendering** — через Next.js App Router
- **Type Safety** — полная типизация TypeScript
- **Atomic Design** — организация UI компонентов

## 🛠️ Технологический стек

### Основные зависимости

- **next** (16.0.1) — React фреймворк с SSR/SSG
- **react** (19.2.0) — библиотека UI
- **react-dom** (19.2.0) — React для DOM
- **tailwindcss** (^4) — utility-first CSS
- **typescript** (^5.9.2) — типизация

### UI библиотеки

- **@radix-ui/react-\*** — доступные UI компоненты
    - `@radix-ui/react-slot` — композиция компонентов
    - `@radix-ui/react-separator` — разделители
- **lucide-react** (^0.553.0) — иконки
- **class-variance-authority** (^0.7.1) — варианты компонентов
- **clsx** (^2.1.1) — условные классы
- **tailwind-merge** (^3.4.0) — слияние Tailwind классов

### Dev зависимости

- **@tailwindcss/postcss** (^4) — PostCSS плагин
- **prettier** (^3.6.2) — форматирование кода
- **prettier-plugin-tailwindcss** — сортировка Tailwind классов
- **eslint** (^9) — линтинг
- **eslint-config-next** — конфигурация ESLint для Next.js

## 📝 Скрипты

```bash
npm run dev          # Разработка
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен сборки
npm run lint         # Линтинг
npm run lint:fix     # Автоисправление
```

## 🎨 Стилизация

Проект использует **Tailwind CSS 4** для стилизации:

- Utility-first подход
- Кастомные цвета и темы через конфигурацию
- Адаптивный дизайн через breakpoints
- PostCSS для обработки стилей

Конфигурация: `postcss.config.mjs`, `tailwind.config.ts` (если есть)

## 📚 Документация по библиотекам

### Next.js

- [Документация](https://nextjs.org/docs)
- App Router, Server Components, SSR/SSG
- Оптимизация изображений, роутинг

### React

- [Документация](https://react.dev/)
- Hooks, Server Components, Concurrent Features
- React 19 с улучшенной производительностью

### Tailwind CSS

- [Документация](https://tailwindcss.com/docs)
- Utility-first CSS фреймворк
- JIT компиляция, кастомизация

### Radix UI

- [Документация](https://www.radix-ui.com/)
- Доступные (a11y) компоненты
- Headless UI, настраиваемые стили

## 🔗 Интеграция с Backend

Frontend взаимодействует с Backend через REST API:

- Базовый URL настраивается через переменные окружения
- Аутентификация через JWT токены (cookies)
- Обработка ошибок и состояний загрузки

## 📦 Переменные окружения

Создайте файл `.env.local` для локальной разработки:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## 🎯 Основные страницы

- `/` — главная страница (дашборд)
- `/not-found` — страница 404
- Другие страницы добавляются в `app/` директорию

## 🔧 TypeScript

Проект полностью типизирован. Пути настроены в `tsconfig.json`:

```json
{
	"paths": {
		"@widgets/*": ["./widgets/*"],
		"@features/*": ["./features/*"],
		"@shared/*": ["./shared/*"],
		"@/*": ["./*"]
	}
}
```

Используйте эти алиасы для импортов:

```typescript
import { Header } from '@widgets/header';

import { Button } from '@shared/ui/button';
```
